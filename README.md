Run following commands:
```termial
pip install -r requirements.txt
```

And run the project upon completion using python enterpreter:
```terminal
python main.py
```

Click activate, to start the app's face recognition.